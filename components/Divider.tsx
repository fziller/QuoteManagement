import { View } from "react-native";

const Divider = () => {
  return (
    <View
      style={{
        borderColor: "#AAAAAA",
        borderWidth: 0.5,
        marginHorizontal: 10,
        marginVertical: 10,
      }}
    />
  );
};

export default Divider;
